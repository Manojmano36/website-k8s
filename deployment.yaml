apiVersion: apps/v1
kind: Deployment
metadata:
  name: website-app
  namespace: default   # 👈 REQUIRED
spec:
  replicas: 2
  selector:
    matchLabels:
      app: website
  template:
    metadata:
      labels:
        app: website
    spec:
      containers:
      - name: website
        image: manojmano36/website-app:
        ports:
        - containerPort: 80
