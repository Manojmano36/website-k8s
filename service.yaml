apiVersion: v1
kind: Service
metadata:
  name: website-service
  namespace: default   # 👈 REQUIRED
spec:
  type: NodePort
  selector:
    app: website
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30085
